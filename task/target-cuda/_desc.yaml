store_global: True        # force
storage_key: target-cuda  # force storage key to avoid mixing up in the future
cache: False              # force

uses:
  - task: setup,a2f9b61079ce4333
    name: cuda,805716c4f32d42cf

env_if_not_used:
  USE_CUDA: 0
  CMETA_TARGET_CUDA: 0

env:
  USE_CUDA: 1
  CMETA_TARGET_CUDA: 1
