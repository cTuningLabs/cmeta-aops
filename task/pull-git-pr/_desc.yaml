authors: Grigori Fursin
copyright: 2025-2026 Grigori Fursin and cTuning Labs. See the COPYRIGHT and LICENSE files in the project root for details.
cache: False                     # force by default

params_map:
  arg2: pr
  arg3: chdir

uses:
  - task: runner,1e48cc0c5f6041a5

  - task: setup,a2f9b61079ce4333
    name: git,959d8c0fbc004bd0

  - task: cmd,c9ba0a88df394d7f
    chdir_and_stay: '{{params.chdir|$None}}'
    cmd: '{{global.git.qpath}} fetch origin pull/{{params.pr}}/head:pr-{{params.pr}}'

  - task: cmd,c9ba0a88df394d7f
    cmd: '{{global.git.qpath}} checkout pr-{{params.pr}}'

