authors: Grigori Fursin
copyright: 2025-2026 Grigori Fursin and cTuning Labs. See the COPYRIGHT and LICENSE files in the project root for details.
store_global: False
storage_key: compile-and-run-program
cache: False
preserve_global_keys:
  - compiler
  - lib-*

params_map:
  arg2: name
  arg3: compute
  arg4: cmd

uses_before_init:
  - task: select-program,beea611608bb459c
    name: '{{params.name|$None}}'
    program_tags: '{{params.program_tags|$None}}'
    program_api_ver: '{{params.program_api_ver|$None}}'
    ask: '{{params.ask|$False}}'
    compute: '{{params.compute|$[]}}'
    cmd: '{{params.cmd|$[]}}'
