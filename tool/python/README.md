--with.venv (on by default)      # detect/create venv
--with.activate (off by default) # add PATH to virtual python)


--with.here (off by default)     # detect python in venv inside current path!
of
---here
