names:
  - clang{{global.host.vars.file_ext_exe}}

match_version: 
  - regex: "clang version\\s*([\\d.]+)"
    group: 1

cmd_get_version: '{{exe_path}} --version'

uses:
- task: setup-tool,a2f9b61079ce4333
  skip_if_not_win: True
  with:
    name: mvcc,e823256bbbe54b4e
