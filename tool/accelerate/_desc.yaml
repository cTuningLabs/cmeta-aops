authors: Grigori Fursin
copyright: 2025-2026 Grigori Fursin and cTuning Labs. See the COPYRIGHT and LICENSE files in the project root for details.
names:
  - accelerate{{global.host.vars.file_ext_exe}}

cache_params_use: 
  all:
    pip-accelerate:
      version: "{{global.pip-accelerate.version}}"
      path: "{{global.pip.path}}"

match_version: 
  - regex: 'version: (.+)'
    group: 1

cmd_get_version: '{{global.pip.cmd}} show accelerate'

uses:
  - task: setup,a2f9b61079ce4333
    name: pip,c4987bc3955347f2
    version: "{{params.version|$None}}"
    with:
      package: accelerate

force_tool_path: "{{global.python.path_bin}}{{global.host.vars.os_sep}}accelerate{{global.host.vars.file_ext_exe}}"

